{
  "name": "finance-dashboard-backend-original",
  "version": "1.0.0",
  "private": true,
  "description": "Finance dashboard backend built with Node.js core modules and MongoDB persistence",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "keywords": [
    "node",
    "finance",
    "rbac",
    "assignment"
  ],
  "license": "MIT",
  "dependencies": {
    "mongodb": "^7.1.1"
  }
}
